// Renders the shared account settings inside the dashboard layout so the
// teacher sidebar stays visible (the public /profile/settings route has none).
export { default } from "@/app/(public)/profile/settings/page";
