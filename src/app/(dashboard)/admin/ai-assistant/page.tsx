// Renders the shared AI assistant inside the dashboard layout so the admin
// sidebar stays visible (the public /ai-assistant route has no sidebar).
export { default } from "@/app/(public)/ai-assistant/page";
